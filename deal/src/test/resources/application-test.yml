spring:
  config:
    activate:
      on-profile: "test"
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  liquibase:
    enabled: false
  jpa:
    hibernate:
            ddl-auto: none
server:
  port: 8080






